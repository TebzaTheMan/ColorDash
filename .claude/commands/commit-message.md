Read `git diff --staged`. If nothing is staged, stop.

Output a single Conventional Commits subject in a code block:
`<type>: <imperative summary>` — lowercase, no period, ≤72 chars.

Types: feat, fix, refactor, perf, docs, test, chore, build, ci, style, revert.

No body. Do not run `git log` or `git commit`.
