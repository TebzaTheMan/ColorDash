Run `git diff --staged` to review staged changes.

Read `docs/index.md` to get the full docs index.

For each doc listed in the index, determine if it is relevant
to the staged changes. Only read the relevant docs.

For each relevant doc, check if the staged changes make it
inaccurate or incomplete.

If updates are needed, make them and summarize what changed and why.
If nothing needs updating, confirm that explicitly.
